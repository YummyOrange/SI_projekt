{% extends 'base.html.twig' %}

{% block title %}{{ 'title.record_view'|trans }} {{ address.id }}{% endblock %}

{% block body %}



    {% if address is defined %}
        <h3>{{ 'label.id'|trans }}: {{ address.id }}</h3>
        <h3>Tytuł</h3>
        <p>{{ address.title }}</p>
{#        <h3>Komentarz</h3>#}
{#        <p>{{ address.comment }}</p>#}
        <h3>{{ 'label.created_at'|trans }}</h3>
        <p>{{ address.createdAt|date('Y-m-d') }}</p>
        <h3>{{ 'label.updated_at'|trans }}</h3>
        <p>{{ address.updatedAt|date('Y-m-d') }}</p>
        <h3>{{ 'label.category'|trans }}</h3>
        <p>{{ address.category.title }}</p>
        <a href="{{ url('address_index') }}" title="{{ 'action.back_to_list'|trans }}">
            {{ 'action.back_to_list'|trans }}
        </a>
    {% else %}
        <p>{{ 'message.record_not_exist'|trans }}</p>
    {%  endif %}

{% endblock %}